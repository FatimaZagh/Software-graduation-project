import 'super_admin_shell.dart';

/// Entry screen for platform Super Admin — delegates to the full management shell.
class SuperAdminDashboardScreen extends SuperAdminShell {
  const SuperAdminDashboardScreen({super.key});
}

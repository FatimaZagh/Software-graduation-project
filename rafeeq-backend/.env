# Rafeeq backend — copy to .env and fill in values
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3-8b-instruct:free
OPENROUTER_HTTP_REFERER=http://localhost:3000
OPENROUTER_APP_TITLE=Rafeeq Medical App

# Rafeeq AI Medical Assistant (POST /api/ai/chat) — get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional
JWT_SECRET=CHANGE_ME_RAFEEQ_JWT_SECRET

# PostgreSQL — pharmacy inventory (Sequelize)
# IMPORTANT: PG_PASSWORD must match the password you set when installing PostgreSQL / pgAdmin login.
PG_USER=postgres
PG_PASSWORD=your_actual_pgadmin_password
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=rafeeq_db

# Optional single connection string (overrides PG_* above when set):
# DATABASE_URL=postgres://postgres:YOUR_PASSWORD@localhost:5432/rafeeq_db
